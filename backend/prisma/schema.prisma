generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "postgresql"
  url      = env("DATABASE_URL")
}

model Guild {
  guildId  String   @id
  modChannel String?
  voiceCategory String?
  voiceLobby String?  
  nbaChannel String?
}
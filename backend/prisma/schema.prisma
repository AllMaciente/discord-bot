generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "postgresql"
  url      = env("DATABASE_URL")
}

model Guild {
  guildId  String   @id
  modChennel String?
  voiceCategory String?
  voiceLobby String?  
  nbaChennel String?
}